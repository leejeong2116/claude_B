/*
 * B_BMS_fan.h
 *
 * 팩 냉각 팬 제어. 원래 B_BMS.c 안에 섞여 있던 것을 분리했다 (BQ 드라이버와 관심사가 다르다).
 *
 * 무엇을 식히나
 * -------------
 * 셀과 FET. 온도원은 각 BQ가 자기 서미스터로 재는 값이다:
 *   - 셀   : TS1(CELL_Temp) · DASTATUS5의 Max/Avg Cell Temp  — 양쪽 보드 모두
 *   - FET  : TS3(FET_Temp)  — TOP(BMS_FET_BOARD)만. 게이트를 구동하는 쪽이 TOP이므로
 * 두 보드에서 나온 값 중 **가장 높은 온도 하나**로 duty를 정한다. 평균이 아니라 최댓값인
 * 이유는, 한 곳만 뜨거워도 식혀야 하는데 평균을 쓰면 그 지점이 묻히기 때문이다.
 *
 * 아직 없는 것 — 태코(회전수) 되먹임
 * -----------------------------
 * 지금 이 파일은 duty 를 "명령"만 하고, 팬이 실제로 도는지는 모른다. CAN 으로 나가는
 * 값도 명령한 duty 다. 팬 선이 빠지거나 날개가 막혀도 코드는 알 수 없다.
 *
 * 하드웨어 현황 (2026-08-21 확인)
 *   - 팬 9WPA0812P4G001 은 4선이고 Sensor(노랑) 선으로 1회전당 펄스 2개를 낸다
 *   - 그런데 보드 커넥터가 J10 = 20037WR-03P(3핀)이라 그 선을 받지 않는다
 *   - 커넥터를 04P 로 바꾸고 Sensor 를 PC13 에 + 풀업 10k 이면 쓸 수 있다 (TODO.md C1)
 *
 * 구현할 때 지킬 것 — 넣는 순간 인터럽트가 생기므로 미리 적어 둔다
 *   1. PC13 = EXTI13, **falling edge 만**. 양 edge 를 잡으면 펄스가 2배로 세어진다.
 *   2. ISR 안에서는 카운터 증가만 한다. 나눗셈·printf·I2C·HAL_Delay 금지.
 *      rpm 환산은 메인 루프에서.
 *   3. NVIC 우선순위를 BQ ALERT(EXTI0/3/6)보다 **낮게** 둔다. 진단이 안전을 밀면 안 된다.
 *   4. 카운터는 volatile 로 두고, 읽고 0으로 되돌리는 구간은 인터럽트를 막고 한다.
 *   5. 시간 기준은 HAL_GetTick() 이 아니라 BMS_RTC_NowMs(). 저전력 진입 전에
 *      HAL_SuspendTick() 이 SysTick 을 멈추기 때문이다.
 *
 * 환산과 판정
 *   rpm = (1초간 펄스 수) x 30        // 1회전 = 펄스 2개 -> n/2 x 60
 *   검산: 8,250 rpm -> 초당 275 펄스 -> 275 x 30 = 8,250
 *   이상 판정: duty > 0 인데 rpm < 300 (20% duty 에서도 2,400 rpm 이 정상)
 *
 * 부하
 *   최대 275 Hz. ISR 이 카운터 하나면 1초에 약 52 us, CPU 점유 0.005%.
 *   이미 도는 SysTick(1,000 Hz)의 1/4 도 안 된다. 폴링은 불가능하다 —
 *   메인 루프가 약 300 ms 주기인데 펄스 간격이 3.6 ms 라 대부분 놓친다.
 *
 * 구속 보호와의 관계
 *   팬에 Locked rotor burnout protection 이 있어 구속돼도 팬 자체는 타지 않고,
 *   막힘이 풀리면 자동 재기동한다. 다만 그 사실을 외부로 알리지는 않으므로
 *   "냉각이 조용히 멈춘 상태"는 태코 없이는 알 수 없다.
 *
 * 제어 곡선 (히스테리시스 포함)
 * -----------------------------
 *   33도 이하 : 정지        <- OFF 임계
 *   33~35도   : 직전 상태 유지 (정지 중이었으면 계속 정지)
 *   35도      : 20%로 기동  <- ON 임계, 최소 duty
 *   35~55도   : 20~100% 선형
 *   55도 이상 : 100%
 * ON/OFF 임계를 2도 벌려 둔 것은 임계 근처에서 팬이 켜졌다 꺼졌다 반복(채터링)하는 것을
 * 막기 위해서다. 최소 duty가 20%인 것은 그보다 낮으면 팬이 기동하지 못하고 멈춘 채
 * 전류만 먹기 때문이다.
 *
 * 온도를 못 읽을 때 (여기가 이 모듈에서 가장 조심할 부분)
 * -------------------------------------------------------
 * 팬 100%는 8.52W다 (9WPA0812P4G001, 12V x 0.71A). MCU 전체가 8.6mW이므로 약 1,000배이고,
 * 무부하로 3일 방치되면 613Wh = 팩(2,880Wh)의 21%를 팬이 먹는다. 그래서 "온도를 모른다"를
 * 두 경우로 나눈다:
 *
 *   한 번도 못 읽음 -> 서미스터 미실장/미배선. 식힐 대상이 뜨겁다는 근거가 없다 -> 정지
 *   읽다가 끊김     -> 센서/통신 고장. 팩이 뜨거운데 모를 수 있다              -> 100%
 *
 * 이 둘을 구조체의 온도 값만으로는 구별할 수 없다. BMS[]가 0으로 초기화되는데 0.0도가
 * 타당 범위(-40~130) 안이라 "정상 온도"로 보이기 때문이다. 그래서 읽기가 성공하고 값이
 * 타당할 때만 서는 BMS_Unit.temp_ever_valid 플래그를 쓴다 (BMS_Fan_NoteTemperature).
 *
 * 또 하나: 읽기가 실패하면 온도 칸은 틀린 값이 되는 게 아니라 **갱신되지 않는다**
 * (드라이버가 성공했을 때만 쓴다). 그래서 통신이 끊겨도 직전 값이 계속 정상으로 보인다.
 * comm_fail_cycles 로 값의 나이를 함께 본다.
 *
 * 하드웨어
 * --------
 * TIM2 CH2 PWM (PA1). TIM2는 팬 전용이므로 시간 측정에 쓰지 말 것 — delayUS()가 DWT
 * 사이클 카운터를 쓰는 이유가 이것이다. STOP1에서 깨어나면 TIM2 PWM이 멎으므로
 * BMS_FanControl_Init()을 다시 불러야 한다 (B_BMS_power_mode.c 가 처리).
 */

#ifndef INC_B_BMS_FAN_H_
#define INC_B_BMS_FAN_H_

#ifdef __cplusplus
extern "C" {
#endif

#include "B_BMS.h"

/* TIM2 PWM 시작. main()에서 한 번, STOP1 복귀 때마다 한 번 더. */
void BMS_FanControl_Init(void);

/* 매 사이클 호출. 전 보드의 온도를 모아 duty를 다시 정한다. */
void BMS_FanControl_Update(void);

/* duty 직접 지정 (0~100). 시험용 — 정상 운전 중에는 Update()가 덮어쓴다. */
void BMS_FanControl_SetDuty(uint8_t duty_percent);
uint8_t BMS_FanControl_GetDuty(void);

/* 0 이면 아직 유효한 온도를 한 번도 못 받았다는 뜻 (서미스터 미실장 의심).
 * CAN SOC 프레임(0x043/0x047)으로 나가므로 밖에서 원인을 구분할 수 있다. */
uint8_t BMS_FanControl_TempEverValid(void);

/* 드라이버가 온도를 성공적으로 읽을 때마다 호출. 값이 타당할 때만 플래그가 선다.
 * 서미스터가 없으면 읽기는 성공해도 값이 범위 밖이라 여기서 걸러진다. */
void BMS_Fan_NoteTemperature(BMS_Unit *unit, float temp_c);

#ifdef __cplusplus
}
#endif

#endif /* INC_B_BMS_FAN_H_ */
