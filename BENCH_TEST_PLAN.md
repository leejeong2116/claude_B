# 벤치 시험 절차서

**목적: 펌웨어의 모든 기능이 의도대로 도는지 확인하고, 코드 상수의 근거가 되는 임계값을 실측한다.**

> ⚠️ **이 문서는 보드 검사가 아니다.** 시험에 쓰는 PCB는 최종 보드와 다르다.
> 여기서 확인하는 것은 **코드**이고, 보드는 코드를 돌릴 플랫폼일 뿐이다.
> 하드웨어 수치(§6·§9)는 최종 보드로 전이되는 조건을 항목마다 적어 두었다.

관련 문서: `EVM_TEST.md`(EVM 기준) · `HARDWARE_VERIFICATION.md`(최종 보드 검증) ·
`MEASUREMENT_MASTER.md`(전체 실측 목록)

---

## 0. 시험 구성

### 장비

| 장비 | 용도 |
|---|---|
| 셀 시뮬레이터 32ch (2세트) | TOP 16S + BOT 16S 동시 구동 |
| 가변전원 (저전압·정밀) | **션트 전압 주입** — §1-3 참조 |
| 전자부하 / 가변전원 | 실제 전류 (§9에서만) |
| 오실로스코프 2ch 이상 | G3VM 지연, 루프 주기 |
| BQStudio + EV2400 | 레지스터 확인, 핀 강제 구동 |
| PCAN + 분석 SW | `0x300` 진단 프레임 관측 |
| 가변저항 (10 kΩ·100 kΩ) ×3 | 서미스터 대체 |
| 멀티미터 | 핀 레벨 확인 |

### 배선

```
      셀 시뮬레이터 세트1 (16ch) ──▶ TOP BQ76972 VC0~VC16
      셀 시뮬레이터 세트2 (16ch) ──▶ BOT BQ76972 VC0~VC16

      가변전원 (mV) ─────────────▶ BOT SRP / SRN     ← 전류 흉내
      가변저항 ×3 ───────────────▶ TS1 / TS3 / HDQ   ← 온도 흉내

      오실로스코프 CH1 ──────────▶ BOT DCHG 또는 DDSG
      오실로스코프 CH2 ──────────▶ TOP CFETOFF 또는 DFETOFF

      EV2400 ────────────────────▶ I2C (BQStudio용, MCU와 배타적)
      PCAN ──────────────────────▶ CAN_H / CAN_L
```

### ⚠️ 안전

- 셀 시뮬레이터 16S면 최대 **67 V**, TOP+BOT 직렬이면 **134 V**다. 저전압이라고 방심하지 말 것.
- TOP과 BOT은 **기준 전위(GND)가 다르다.** 오실로스코프 프로브 접지를 잘못 물리면
  두 GND가 단락되어 보드가 죽는다. **차동 프로브를 쓰거나, 한 번에 한쪽만 측정할 것.**
- BQStudio(EV2400)와 MCU가 **같은 I2C 버스를 동시에 쓰면 안 된다.** 한 번에 하나만.

---

## 1. 핵심 기법 네 가지

이걸 알면 시험이 훨씬 쉬워진다.

### 1-1. 전압 보호 — 셀 시뮬레이터로 직접

COV / CUV / SOV / SUV 전부 시뮬레이터 전압만 바꾸면 된다. 가장 쉽다.

### 1-2. 온도 보호 — 가변저항 + BQStudio로 맞추기

`TS1Config = 0x07` 이 **18 kΩ NTC 모델**을 쓰므로, 이론상 온도별 저항은 아래와 같다
(B=3950 가정 — **실제 서미스터의 B값에 따라 달라진다**).

| 목표 온도 | 대략 저항 | 무슨 보호 |
|---|---|---|
| −20 ℃ | ~190 kΩ | UTD |
| 0 ℃ | ~60 kΩ | UTC |
| 25 ℃ | 18 kΩ | (기준) |
| 60 ℃ | ~4.5 kΩ | OTC |
| 65 ℃ | ~3.8 kΩ | OTD |
| 100 ℃ | ~1.3 kΩ | OTF |

> 💡 **계산하지 말고 이렇게 해라**: 가변저항을 돌리면서 **BQStudio에서 온도값을 실시간으로
> 읽는다.** 원하는 온도가 나올 때까지 돌리면 끝이다. B값을 몰라도 되고 훨씬 정확하다.

### 1-3. 전류 보호 — **200 A 안 흘려도 된다** ★

BQ는 전류를 직접 재지 않는다. **션트 양단 전압(SRP−SRN)** 을 본다.
그러니 션트에 실제 전류를 흘리는 대신 **SRP/SRN에 mV 전압을 직접 주입**하면 된다.

```
션트 400 µΩ 기준 (V = I × R)

  SCD   200 A  →  80 mV        OCD3  −65 A  →  −26 mV
  OCD2  125 A  →  50 mV        OCC    50 A  →   20 mV
  OCD1   65 A  →  26 mV        무부하 <1 A  →   <0.4 mV
```

전부 **mV 단위**다. 가변전원 저전압 모드나 저항 분압으로 만들 수 있다.

**주입 방법**: 션트를 떼거나(가능하면), 아니면 SRP/SRN에 직접 전압원을 물린다.
전류가 흐르지 않으므로 안전하고, 200 A 전원도 필요 없다.

> ⚠️ **극성이 핵심이다.** 부호가 반대면 충전을 방전으로 읽어 엉뚱한 보호가 걸린다.
> §3-3에서 이것부터 확인한다.

### 1-4. 시간·임계를 **줄여서** 확인 — BQ는 절대 건드리지 않는다 ★★

일부 기능은 그냥 두면 시험이 불가능하다.

| 기능 | 그냥 하면 | 어떻게 |
|---|---|---|
| STOP1 진입 | **1시간** 대기 | 상수를 30초로 |
| SHUTDOWN 진입 | **3일** 대기 | 상수를 90초로 |
| OCV 재기준 | 10분 대기 | 30초로 |
| 감시자 소프트 백업 | **영영 안 뜸** — BQ가 먼저 자름 | **MCU 임계를 BQ보다 낮게** |

#### 원칙: 낮추는 건 **MCU 쪽만.** BQ 보호 설정은 시험 내내 그대로 둔다.

소프트 백업을 띄우려고 *"BQ 보호를 끄면 되지 않나"* 싶지만 **하면 안 된다.**
그건 진짜 보호막을 걷어내고 시험하는 것이다. 대신 MCU 임계를 BQ보다 **아래로** 내리면
같은 코드 경로가 그대로 시험되면서 **BQ 보호는 살아 있다.**

```
BQ COV 4.20 V ────────────────────  ← 끝까지 유지 (진짜 보호막)
MCU 소프트 4.25 V → 4.00 V ↓         ← 이것만 내림
                    ↑ 여기서 감시자가 먼저 반응 → 사유 7 확인
```

> 🚨 **원복이 이 방식의 유일한 위험이다.** 임시로 고치는 곳이 총 6~8군데이고
> 하나라도 놓치면 실차에서 사고가 난다. **§11에 원복표와 기계적 확인 방법**을 두었다.

---

## 2. 단계 1 — 통신이 되는가

**여기가 안 되면 아래가 전부 무의미하다.**

### 절차
1. 셀 시뮬레이터를 TOP/BOT 모두 **3.700 V 균일**로 설정
2. 전원 인가, 디버거 연결

### 확인

| 확인 | 기대값 | 실측 |
|---|---|---|
| `LV_BMS_initOK` | 1 | ______ |
| `RX_CRC_Fail` | **0 유지** | ______ |
| `I2C_HAL_Fail` | **0 유지** | ______ |
| `BMS[TOP].comm_fail_cycles` | 0 | ______ |
| `BMS[BOT].comm_fail_cycles` | 0 | ______ |
| CAN에 프레임이 나오는가 | `0x040`~`0x046`, `0x300` | ______ |

**실패하면**: `CommType`(I2C basic) 확인, I2C 주소(둘 다 `0x10`), 배선
(TOP=`hi2c2` PB13/PB14, BOT=`hi2c4` PB6/PB7). `CRC_Mode`는 끄지 말 것.

---

## 3. 단계 2 — 값을 맞게 읽는가

**PR #32~#40에서 고친 버그들이 실제로 해결됐는지 보는 단계다.**

### 3-1. 셀 전압

- [ ] `CellVoltage[0..15]` 전부 **3700 ± 오차** (단위 mV)
- [ ] 셀 하나만 3.500 V로 → `MinCellVolatge`가 3500으로 따라감
- [ ] CAN `0x048`~`0x04B`(BOT) / `0x051`~`0x054`(TOP)에 같은 값

### 3-2. 스택 전압 ★ (32767 포화 버그 검증)

16 × 3.700 V = **59.2 V**

- [ ] `Stack_Voltage` ≈ **59200** (mV)
- [ ] **32767 근처에 붙어 있으면 실패** → `DAConfiguration`이 `0x06`이 아닌 것 (§4로)
- [ ] `Battery_Voltage_Sum × 10 ÷ 16` ≈ 3700
- [ ] CAN `0x041`/`0x045`도 동일

> 이전 코드는 여기가 32767에 포화돼 있었다. **이 항목이 핵심 검증이다.**

### 3-3. 전류 부호 ★ (다른 시험의 전제)

- [ ] SRP/SRN에 **+20 mV** 주입 → `Pack_Current` ≈ **+5000** (= +50 A, **충전 = 양수**)
- [ ] **−20 mV** → ≈ **−5000**
- [ ] 부호가 반대면 **SRP/SRN 배선을 바꿀 것.** 그러지 않으면 §5 전류 보호가 전부 반대로 걸린다
- [ ] `BMS[TOP].Pack_Current`가 sync 후 BOT과 같아지는가
- [ ] CAN `0x046`(TOP) 전류 필드는 **0**인가 (의도된 동작)

### 3-4. 온도

- [ ] 가변저항 3개를 18 kΩ 근처로 → 세 채널 모두 25 ℃ 근처
- [ ] **TOP의 `FET_Temp`가 실제로 변하는가** ← TS3=FET온도 변경의 검증
- [ ] `Max/Min/Avg_Cell_Temp`가 그럴듯한가

---

## 4. 단계 3 — 설정이 실제로 칩에 써졌는가

`BQ769x2_SetRegister()`는 **실패해도 알려주지 않는다.** 확인할 방법은 읽어보는 것뿐이다.

### 절차
1. MCU 전원을 끄거나 I2C를 분리 (BQStudio와 버스 충돌 방지)
2. EV2400 연결 → BQStudio → Data Memory → `.gg.csv` export
3. TOP / BOT **각각** 뜬다

### 대조표

| 레지스터 | TOP | BOT | 왜 |
|---|---|---|---|
| `DA Configuration` | **0x06** | **0x06** | `0x02`면 전압 포화 |
| `CFETOFF Pin Config` | **0x2A** | 0x07 | TRM Table 13-6 확인 완료 |
| `DFETOFF Pin Config` | **0x2A** | **0x6A** | TOP이 BOTHOFF면 안 됨 (bit6) |
| `DCHG Pin Config` | 0x07 | 0x22 | |
| `DDSG Pin Config` | 0x07 | 0x22 | |
| `TS3 Config` | **0x0F** | 0x07 | OTF의 유일한 소스 |
| `CHG FET Protections A/B/C` | 0x98 / 0xD5 / 0x56 | 동일 | 감시자가 같은 값 가정 |
| `DSG FET Protections A/B/C` | 0xE4 / 0xE6 / 0xE2 | 동일 | |
| `Enabled Protections A/B/C` | 0xFC / 0xF7 / 0xF6 | 동일 | |
| `Vcell Mode` | 0x0000 | 0x0000 | 16셀 |

**다르면**: 쓰기가 실패한 것이다. `BQ769x2_Init()` 안의 `delayUS()` 를 늘려보고,
`SET_CFGUPDATE` / `EXIT_CFGUPDATE` 가 제대로 나가는지 I2C를 스코프로 확인.

---

## 5. 단계 4 — 루프 주기 실측 ★★

**`B_BMS_protect.c`의 모든 시간 임계가 이 값 하나에서 유도된다.**
현재 `BMS_LOOP_PERIOD_MS_EST = 300` 은 **계산으로 나온 추정치**다.

### 방법 A — CAN 프레임 간격 (제일 쉬움)

PCAN 분석기에서 `0x042` 프레임의 **연속 도착 시각 차이**를 본다.

### 방법 B — GPIO 토글 + 스코프 (더 정확)

`main.c` while 루프 맨 앞에 남는 GPIO를 토글하는 코드를 임시로 넣고 주기를 잰다.

```c
HAL_GPIO_TogglePin(BOT_BLUE_GPIO_Port, BOT_BLUE_Pin);   // 임시
```
(BLUE LED는 감시자 표시용이라 시험 후 반드시 원복)

### 기록

| 조건 | 실측 |
|---|---|
| 부하 있음 (전류 주입 상태) | ______ ms |
| 무부하 (전류 0) | ______ ms |

- [ ] `BMS_LOOP_PERIOD_MS_EST`를 **부하 있을 때 값**으로 수정 후 재빌드
- [ ] 수정 후 §7 감시자 시험을 다시 (임계가 바뀌었으므로)

### 덤으로 — 응답성 개선 여지

주기가 길면 `DirectCommands()`의 `delayUS(2000)` 이 원인이다 (보드당 40회면 80 ms).
TI 예제에서 그대로 가져온 값이라 더 짧아도 될 수 있다.

- [ ] `delayUS(2000)` → 1000 → 500 으로 줄이며 `RX_CRC_Fail`이 늘지 않는지 확인
- [ ] 늘면 **즉시 원복** (통신 안정성이 응답성보다 우선)

> **전이 조건**: 최종 보드가 **같은 MCU·같은 클럭·같은 I2C 속도**면 이 값이 그대로 쓰인다.
> 다르면 재측정.

---

## 6. 단계 5 — 보호 트립

각 항목마다 **"어느 FET이 꺼져야 하는가"** 가 다르다. 그게 이 시험의 핵심이다.

| # | 보호 | 만드는 법 | CHG | DSG | 지연 |
|---|---|---|:---:|:---:|---|
| 6-1 | **COV** | 셀 하나 → 4.25 V | **OFF** | ON 유지 | 1 s |
| 6-2 | **CUV** | 셀 하나 → 2.4 V | ON 유지 | **OFF** | 1 s |
| 6-3 | **OCC** | SRP/SRN → **+20 mV** | **OFF** | ON 유지 | 426 ms |
| 6-4 | **OCD1** | → **−26 mV** | ON 유지 | **OFF** | 426 ms |
| 6-5 | **OCD2** | → **−50 mV** | ON 유지 | **OFF** | 9.9 ms |
| 6-6 | **SCD** | → **−80 mV** | **OFF** | **OFF** | 450 µs |
| 6-7 | **OTC** | TS1 → 60 ℃ | **OFF** | ON 유지 | 5 s |
| 6-8 | **OTD** | TS1 → 65 ℃ | ON 유지 | **OFF** | 5 s |
| 6-9 | **OTF** | **TOP TS3** → 100 ℃ | **OFF** | **OFF** | 5 s |
| 6-10 | **UTC** | TS1 → −5 ℃ | **OFF** | ON 유지 | 5 s |
| 6-11 | **UTD** | TS1 → −25 ℃ | ON 유지 | **OFF** | 5 s |

### 매 항목마다 확인할 것

- [ ] `SafetyStatus A/B/C` 의 해당 비트가 서는가 (CAN `0x05A`/`0x06D`)
- [ ] **`BMS[TOP].CHG` / `.DSG` 가 위 표대로 되는가** ← 선택적 차단
- [ ] CAN `0x300` byte1 — **b2/b3(기대)는 서고 b4/b5(실측)는 꺼져야 정상**
- [ ] CAN `0x300` byte0 이 **0을 유지**하는가 (감시자가 개입하면 안 됨)
- [ ] 조건 해제 시 복구되는가

> **6-9 OTF는 특별하다.** TS3을 FET 온도로 바꾸기 전에는 **아예 트립될 수 없었다.**
> 이 항목이 그 수정의 검증이다. TOP에서만 가능하다 (BOT에는 FET 온도 채널이 없음).

### 6-12. OCDL / SCDL 자동 복구 ★

한 번 걸리면 명시적 복구 명령 없이는 **영구 락아웃**되는 보호다.

- [ ] −26 mV 를 **2회 이상 반복** 인가 (`OCDLLatchLimit`=2) → `SafetyStatusC` bit5
- [ ] 0 mV 로 되돌린 뒤 **약 5초 내 자동 복구**되는가 (`recover_latched_protections()`)
- [ ] 복구 명령이 무한 반복되지 않는가 (BQStudio I2C 트래픽)
- [ ] −80 mV 반복으로 SCDL도 동일하게

### 6-13. PF latch

- [ ] 셀 하나 → **4.35 V** (SOV 4.3 V) 또는 **2.1 V** (SUV 2.2 V)
- [ ] `PF_ProtectionsTriggered` = 1
- [ ] CAN `0x300` byte0 = **5**, byte1 b1(latch) = **1**
- [ ] **조건 해제해도 BOTHOFF가 안 풀리는가** ← PF는 자동 복구 금지
- [ ] `PF_RESET`이 자동으로 나가지 않는가 ← **나가면 심각한 버그**

> PF 시험 후에는 BQ를 리셋하거나 BQStudio로 `PF_RESET`을 **수동** 전송해야 다음 시험이 된다.

---

## 7. 단계 6 — 차단 경로와 감시자 ★★

### 7-1. 선택적 차단 실동작 — 이 설계의 핵심

BQStudio에서 BOT에 서브커맨드를 보내 핀을 강제로 올린다.

| 보낼 것 | 대상 | 기대 |
|---|---|---|
| `DCHG_HI` (0x2817) | BOT | TOP.CFETOFF High → **TOP CHG=0, DSG는 1 유지** |
| `DCHG_LO` (0x2807) | BOT | 원복 |
| `DDSG_HI` (0x2818) | BOT | TOP.DFETOFF High → **TOP DSG=0, CHG는 1 유지** |
| `DDSG_LO` (0x2808) | BOT | 원복 |

- [ ] `DCHG_HI` → TOP.CFETOFF 핀 전압 High (멀티미터/스코프)
- [ ] TOP `FETStatus` CHG 비트 = 0
- [ ] **DSG 비트는 1 유지** ← **여기가 제일 중요**
- [ ] `DDSG_HI` → DSG만 0, CHG는 1 유지

> 🔴 **DSG까지 같이 꺼지면 `TOP.DFETOFF`가 BOTHOFF로 잡혀 있는 것이다.** §4로 돌아가
> `DFETOFF Pin Config`의 bit6을 확인할 것 (0이어야 함).

### 7-2. G3VM 전파 지연 실측 ★★

**이 한 값이 두 가지를 동시에 결정한다.**

| 결정하는 것 | 판정 기준 |
|---|---|
| **SCD가 성립하는가** | 총 차단시간 < FET SOA 허용시간 (보고서 5장: 140 V·100 A에서 **50 µs**) |
| **감시자가 오탐을 내는가** | `PATH_FAULT_CYCLES`(현재 600 ms)가 이 지연의 **2~3배 이상**이어야 함 |

**측정**: 오실로스코프 2채널

```
CH1 ──▶ BOT DDSG 핀
CH2 ──▶ TOP DFETOFF 핀        (여유 되면 CH3에 TOP DSG FET 게이트)
```

BQStudio로 `DDSG_HI` 를 보내고 두 파형의 시간차를 잰다.

| 항목 | 실측 |
|---|---|
| t_on (LOW→HIGH 전파) | ______ µs |
| t_off (HIGH→LOW 전파) | ______ µs |
| (있으면) 게이트 방전 완료까지 | ______ µs |
| **총 차단시간** = SCD Delay 450 µs + BQ 반응 + 위 값 | ______ µs |

- [ ] **50 µs SOA 대비 판정** → 넘으면 SCD 임계 하향 또는 2차 보호기/퓨즈에 명시적 위임
- [ ] `PATH_FAULT_CYCLES` ≥ 실측 지연 × 2~3 인지 확인. 아니면 상수 조정

> **전이 조건**: 최종 보드가 **같은 G3VM 부품**을 쓰면 이 값이 그대로 쓰인다
> (소자 특성이지 보드 특성이 아니다). 부품이 바뀌면 재측정.
>
> 💡 PCB가 없어도 **G3VM 단품을 브레드보드에 올려** LED 쪽(1·2번)에 펄스를 넣고
> 출력(3·4번)을 보면 하한은 바로 나온다.

### 7-3. BOTHOFF 경로

- [ ] MCU PA15 Low→High → **TOP CHG=0, DSG=0 (둘 다)**
- [ ] High→Low → 둘 다 복귀
- [ ] **MCU 리셋 버튼을 누르는 동안** BOT_DFETOFF가 High로 뜨는가 ← 페일세이프
- [ ] 전원 인가 직후, BQ 초기화가 끝날 때까지 BOTHOFF가 유지되는가

### 7-4. 감시자 — 통신 두절

- [ ] 동작 중 한쪽 I2C 분리
- [ ] 약 1.5초 후 CAN `0x300` byte0 = **4**, byte1 b0 = 1
- [ ] BOT_DFETOFF High, **BLUE LED 점등**
- [ ] 다시 연결 → 약 5초 후 자동 복구, byte2(재시도) = 1
- [ ] **3회 반복 → latch** (byte0 = 9, byte1 b1 = 1)

### 7-5. 감시자 — 크로스체크 오탐 확인 ★

§6의 보호 트립을 하는 동안 계속 관찰:

- [ ] CAN `0x300` byte3/byte4 (mismatch cycles)가 **임계 미만**에 머무는가
- [ ] byte0이 **2 또는 3(경로 고장)으로 뜨지 않는가**

> 🔴 **뜬다면 G3VM 지연이 `PATH_FAULT_CYCLES`보다 길다는 뜻이다.** §7-2 결과로 상수를 조정할 것.

### 7-6. 감시자 개입 중 슬립 금지

- [ ] 전류 0 + 통신 두절 상태를 만듦
- [ ] MCU가 **안 자고 계속 도는가** (CAN 프레임이 계속 나오는지)
- [ ] 통신 복구 후에는 정상적으로 슬립에 드는가

### 7-7. 소프트 백업 오탐 확인

- [ ] §6 전 과정에서 CAN `0x300` byte0이 **7 또는 8로 뜨지 않는가**
- [ ] BQ가 먼저 트립하면 소프트 카운터가 0으로 유지되는가

> 뜬다면 감시자가 BQ를 앞지른 것이다. §5(루프 주기)를 먼저 확정해야 한다.

### 7-8. 감시자 나머지 3사유 강제 유발 ★★

7-4~7-7로는 트립 사유 9개 중 **4개**(1·2·3·4·9)만 확인된다. 나머지를 일부러 띄운다.

#### 왜 그냥은 안 뜨나 — 그리고 **BQ를 끄면 안 되는 이유**

소프트 백업(7·8)은 *"BQ가 죽었을 때"* 를 위한 그물이라, **BQ가 정상이면 설계상 절대 안 뜬다.**

| | BQ가 자르는 값 | MCU 소프트 백업 | 누가 먼저 |
|---|---|---|---|
| 셀 과전압 | **4.1998 V** (`COVThreshold` 83) | 4.250 V | **BQ** |
| 셀 온도 | **65 ℃** (`OTDThreshold`) | 70 ℃ | **BQ** |

BQ가 더 낮은 값에서 먼저 자르므로 MCU 임계까지 도달하지 못한다.
**BQ 보호를 끄는 방식으로 풀지 말 것** — 진짜 보호막을 걷어내고 시험하는 꼴이다.

**대신 MCU 쪽 임계만 BQ보다 낮게 내린다.** BQ 설정은 손대지 않는다.

```c
// Core/Src/B_BMS_protect.c — 시험용 임시 (BQ 레지스터는 그대로)
#define SOFT_CELL_OV_mV   4000U     // 4250 → 4000  (BQ 4.20 V보다 낮게)
#define SOFT_TEMP_MAX_C   40.0f     // 70   → 40    (BQ 65 ℃보다 낮게)
```

> 이 방식이면 시험 내내 **BQ 보호가 4.20 V / 65 ℃에서 살아 있다.**
> 감시자가 안 뜨더라도 BQ가 받아준다.

#### 7-8-1. SOFT_CELLV (사유 **7**)

- [ ] 위 임시 상수로 빌드
- [ ] 셀 시뮬레이터 한 채널을 **4.05 V** 로 (MCU 4.00 초과, BQ 4.20 미만)
- [ ] BQ SafetyStatus COV가 **안 뜨는지** 먼저 확인 (BQStudio)
- [ ] 3초 후 CAN `0x300` byte0 == **7**
- [ ] `0x300` byte1 b0(BOTHOFF) == 1
- [ ] 전압을 3.7 V로 되돌림 → 5초 후 해제되는가

#### 7-8-2. SOFT_TEMP (사유 **8**)

- [ ] 서미스터 저항을 **45 ℃ 상당**으로 (MCU 40 초과, BQ 65 미만)
- [ ] BQ OTD가 **안 뜨는지** 확인
- [ ] **10초** 후 `0x300` byte0 == **8** ← 전압(3초)보다 길다

#### 7-8-3. STACK_MISMATCH (사유 **6**) — 코드 수정 불필요

셀합과 스택 전압이 어긋나면 뜬다. **임시 상수도 BQ 설정도 안 건드린다.**

- [ ] 셀 시뮬레이터 채널 **하나만 분리** (또는 0 V로)
- [ ] 셀합은 줄고 스택 전압(0x34)은 그대로 → 괴리 발생
- [ ] 3초 후 `0x300` byte0 == **6**, byte5(StackMismatchCycles)가 올라가는가
- [ ] 채널 복구 → 해제되는가

> 이 시험이 §3-2(스택 전압 32767 포화 버그)와 짝이다. 포화가 남아 있으면
> 정상 상태에서도 괴리가 생겨 **항상 6이 뜬다.** 그러면 §3-2부터 다시.

---

## 8. 단계 7 — 나머지 기능

### 8-1. SOC ★

SOC는 **두 갈래**로 계산된다. 둘을 따로 확인해야 어느 쪽이 틀렸는지 안다.

```
(a) 쿨롱 카운팅  ← 평상시. BQ의 AccumulatedCharge 적분값 델타 / 팩용량
(b) OCV 재기준   ← 10분 이상 무부하면 (a)를 버리고 전압으로 덮어씀
```

#### 8-1-1. OCV 경로 (b) — 전압만으로

전류 0을 유지하면 부팅 직후 OCV로 시드된다.

| 셀 시뮬레이터 | 기대 SOC | 실측 | 판정 |
|---|---|---|---|
| 2.500 V | **0 ‰** (표 하한) | ______ | |
| 3.300 V | 낮음 (~10 %) | ______ | |
| 3.700 V | 중간 (~60 %) | ______ | |
| 4.100 V | 높음 (~95 %) | ______ | |
| 4.200 V | **1000 ‰** (표 상한) | ______ | |

- [ ] 부팅 SOC가 **0이 아닌가** ← 이전엔 항상 0이었다 (수정된 버그)
- [ ] **단조성** — 전압을 올리면 SOC가 반드시 증가하는가 (역전 0회)
- [ ] 양 끝값(2.5 V→0, 4.2 V→1000)이 **정확히** 나오는가 ← 표 끝점은 데이터시트값이라 오차 없어야 함
- [ ] 4.2 V 초과·2.5 V 미만에서 **1000/0을 넘지 않는가** (클램프)

> 중간 구간(3.3·3.7·4.1 V)은 **근사치**다. 50S 데이터시트에 방전 곡선이 없어
> 일반 고출력 NMC 곡선을 끼워 넣은 것. **"정확히 60 %"를 기대하지 말고
> 범위와 단조성만 본다.** 실측 OCV로 대체하는 절차는 `HARDWARE_VERIFICATION.md` §3-1.

#### 8-1-2. 쿨롱 경로 (a) — 전류를 흘려서

§9(실제 전류)와 같이 하거나, SRP/SRN에 전압을 주입해서 확인한다.

- [ ] 전류 주입 → `AccumulatedCharge_Int`(CAN `0x04E`)가 **변하는가**
- [ ] 방전 방향 전류 → SOC **감소** / 충전 방향 → **증가** (부호가 §3-3과 일치)
- [ ] **정량 확인**: 25 Ah 팩에서 2.5 A를 6분 흘리면 0.25 Ah = **약 1 %** 감소
      실측 감소량 ______ ‰
- [ ] 전류 0으로 되돌리면 SOC가 **멈추는가** (계속 흐르면 오프셋 문제)

> 팩 용량이 **25 Ah**(5000 mAh × 5P)로 맞는지가 여기서 드러난다.
> 20 Ah로 잘못 잡혀 있으면 감소량이 **1.25 배**로 나온다.

#### 8-1-3. OCV 재기준 전환 (a)→(b)

10분은 기다리기 길다. 임시로 줄여서 확인한다.

```c
// Core/Src/B_BMS_soc.c — 시험용 임시
#define BMS_SOC_OCV_REST_MS (30UL * 1000UL)   // 10분 → 30초
```

- [ ] 전류를 흘려 SOC를 쿨롱값으로 벌려 놓음 (예: 전압상 60 %인데 SOC 40 %)
- [ ] 전류 0으로 두고 **30초 대기**
- [ ] SOC가 **OCV 기준값으로 점프**하는가
- [ ] 점프가 **한 번만** 일어나는가 (매 주기 덮어쓰면 쿨롱 경로가 무의미)

#### 8-1-4. 팩 단위 확인

- [ ] TOP·BOT의 `SOC_Permille`이 **같은 값**인가 (`BMS_SyncSharedHardwareData` 후)
- [ ] CAN `0x043` byte0~1 == 그 값인가
- [ ] **`0x047`·`0x233`이 안 나오는가** ← 중복 프레임 제거 확인

### 8-2. RTC

- [ ] `BMS_RTC_ClockSource()` == **1 (LSE)** — 2면 LSI 폴백 (오차 ±5 %)
- [ ] 무부하 방치 시 **30초 주기**로 깨어나는가 (CAN 프레임 간격)
- [ ] `no_load_time_ms`가 실제 경과 시간과 맞는가

### 8-3. 팬

임계는 `B_BMS.c` 상단: **OFF 33 ℃ / ON 35 ℃ / FULL 55 ℃, duty 20~100 %.**

#### 8-3-1. 페일세이프 (제일 중요)

- [ ] **서미스터 3개 전부 분리하고 부팅** → 팬 **정지** (미실장 판정)
- [ ] 정상 부팅 후 서미스터 분리 → 팬 **100 %** (센서 고장 판정)
- [ ] CAN `0x043` byte3(온도유효)가 위 두 경우를 **구분**하는가 (0 / 1)

> 이 둘이 갈리는 게 `fan_temp_ever_valid` 로직이다. **한 번도 못 읽었으면 미실장(정지),
> 읽다가 끊기면 고장(100 %).** 반대로 나오면 로직이 뒤집힌 것.

#### 8-3-2. 곡선 ★ — 가변저항으로 온도를 훑는다

| 설정 온도 | 기대 duty | 실측 | 근거 |
|---|---|---|---|
| 30 ℃ | **0 %** | ______ | OFF 임계 아래 |
| 36 ℃ | ~20 % | ______ | ON 직후 = 최소 duty |
| 45 ℃ | ~50 % | ______ | 35~55 선형 중간 |
| 55 ℃ | **100 %** | ______ | FULL 도달 |
| 60 ℃ | **100 %** | ______ | 초과해도 100 유지 |

- [ ] 위 5점 측정 (PWM은 스코프 또는 CAN `0x043` byte2)
- [ ] 35→55 구간이 **선형**인가 (중간값이 대략 직선 위)

#### 8-3-3. 히스테리시스 ★ — 33 vs 35

- [ ] 온도를 **34 ℃** 로 맞춤 (33과 35 사이)
- [ ] **올리면서** 34 ℃ 도달 → 팬 **정지 유지** (35 넘어야 켜짐)
- [ ] **내리면서** 34 ℃ 도달 → 팬 **동작 유지** (33 밑으로 가야 꺼짐)
- [ ] 34 ℃ 부근에서 팬이 **깜빡이지 않는가** ← 히스테리시스가 없으면 여기서 채터링

### 8-4. CAN

- [ ] 비트레이트 **500 kbit/s** 로 붙는가
- [ ] `0x040`~`0x046` + `0x300` — **매 주기 7 프레임**
- [ ] **`0x047` 이 안 나오는가** ← 중복 제거 확인
- [ ] 바이트 순서가 빅엔디안인가

#### ⚠️ 지금 test data 프레임은 **안 나온다**

`BMS_CAN_SendTestData()` 는 `T_FDCAN_Send_BMS_Data()` 안에만 있는데,
**그 함수를 부르는 곳이 아무 데도 없다** (main 루프는 `SendRunData`·`SendProtectDiag`만 호출).

따라서 **셀 전압(`0x048`~) · 온도(`0x04C`~) · SafetyStatus(`0x05A`) · 쿨롱카운터(`0x04E`~)
· CB status · CUV/COV 스냅샷이 전부 CAN에 안 나온다.**

시험 중 이 값들이 필요하면 둘 중 하나:

| 방법 | 방식 |
|---|---|
| **A. BQStudio로 본다** | 코드 안 건드림. 대신 MCU가 읽은 값이 아니라 **칩이 가진 값**을 보는 것 |
| **B. 임시 호출 추가** | `main.c` 루프에 아래 2줄. MCU 파싱까지 검증됨 |

```c
// main.c — 시험용 임시 (BMS_CAN_SendProtectDiag() 다음)
T_FDCAN_Send_BMS_Data(TOP);
T_FDCAN_Send_BMS_Data(BOT);
```

- [ ] B를 썼다면: `0x050`/`0x059` 가 **DLC 6**인가
- [ ] B를 썼다면: 프레임 수가 늘어 **루프 주기(§5)가 변하지 않았는가** ← 변했으면 §5 재측정
- [ ] **시험 후 원복** (§11)

### 8-5. 슬립 · 셧다운 ★★

`Enter_Sleep_Sequence()` 가 무부하 시간에 따라 **3단계**로 내려간다.

```
전류 있음        → HAL_Delay(63 ms)          루프 페이싱만
무부하           → MCU SLEEP                 (WFI, RTC 30초 웨이크)
무부하 1시간     → MCU STOP1                 깨면 SystemClock_Config + 팬 재초기화
무부하 3일       → BQ SHUTDOWN + MCU SHUTDOWN
```

#### 8-5-1. 시험용 임시 상수 — 1시간·3일을 기다릴 수 없다

```c
// Core/Src/B_BMS_power_mode.c — 시험용 임시
#define TIME_1_HOUR_MS   30000UL     // 1시간 → 30초
#define TIME_3_DAYS_MS   90000UL     // 3일   → 90초
```

> 🚨 **반드시 원복.** 안 되돌리면 주행 중 잠깐 무부하일 때 90초 만에 셧다운된다.
> §11 원복표에 있다.

#### 8-5-2. SLEEP 진입과 웨이크

- [ ] 무부하(`|Pack_Current| < 1 A`)로 만듦
- [ ] CAN 프레임 간격이 63 ms → **30초**로 벌어지는가 (RTC 웨이크 주기)
- [ ] 전류를 다시 흘림 → 다음 웨이크에서 **63 ms로 복귀**하는가
- [ ] 슬립 중 **BQ ALERT 핀을 강제로 흔들면** 30초 안 기다리고 깨는가
      (`Handle_Wakeup_Event()` 경로)
- [ ] 깬 직후 CAN 값이 **정상**인가 (쓰레기값 아님)

#### 8-5-3. STOP1 진입 (30초 후)

- [ ] 무부하 30초 유지 → STOP1 진입
- [ ] **소비 전류 측정**: 진입 전 ______ mA → 진입 후 ______ mA
- [ ] 깨어난 뒤 **CAN이 다시 나오는가** ← `SystemClock_Config()` 재호출 확인
- [ ] 깨어난 뒤 **팬이 다시 도는가** ← `BMS_FanControl_Init()` 재호출 확인 (TIM2 PWM 복구)
- [ ] 깨어난 뒤 I2C가 살아 있는가 (BQ 값이 갱신되는가)

> 팬이 안 돌면 STOP1에서 TIM2가 죽은 채 복구가 안 된 것이다. **이게 STOP1의 대표 함정.**

#### 8-5-4. SHUTDOWN 진입 (90초 후)

- [ ] 무부하 90초 유지 → SHUTDOWN 진입
- [ ] **CAN이 완전히 멈추는가**
- [ ] **BQ도 SHUTDOWN 됐는가** — BQStudio에서 통신 두절 / `BattStat` 확인
- [ ] 소비 전류 ______ µA
- [ ] **깨우기**: 리셋 또는 웨이크업 핀 → 정상 부팅되는가
- [ ] 깬 뒤 SOC가 **날아가지 않았는가** (부팅 시 OCV 시드)

#### 8-5-5. 슬립 금지 조건 (7-6과 짝)

- [ ] 감시자가 BOTHOFF 중일 때 **안 자는가** (7-6에서 확인)
- [ ] PF latch 상태에서 **안 자는가**

#### 8-5-6. ⚠️ 호출되지 않는 함수 3개

아래는 정의만 있고 **부르는 곳이 없다** (전수 확인함):

| 함수 | 위치 |
|---|---|
| `BQ769x2_LOWV_SHUTDOWN()` | `B_BMS_init.c` |
| `BQ769x2_Hard_Shutdown()` | `B_BMS_init.c` |
| `BQ769x2_Wake_Up()` | `B_BMS_init.c` |

- [ ] 이 시험에서 **의도적으로 쓸 것인지** 판단 → 쓸 거면 호출 추가, 아니면 삭제

> 죽은 코드로 남겨 두면 다음 사람이 "이게 동작한다"고 오해한다. **어느 쪽이든 결론을 낼 것.**

### 8-6. LED (`LV_STAT`)

| 색 | 조건 |
|---|---|
| 초록 | 정상 |
| 빨강 | BQ 보호 트립 |
| 파랑 | MCU가 BOTHOFF 어서트 |
| TOP 빨강 | latch (PF / 재시도 초과) |

- [ ] 정상 상태 → **초록**
- [ ] §6에서 BQ 트립 → **빨강**
- [ ] §7-4에서 감시자 개입 → **파랑**
- [ ] §6-13 PF → **TOP 빨강**, 전원 재인가해도 유지되는가

### 8-7. 셀 밸런싱

- [ ] 셀 시뮬레이터로 **한 채널만 50 mV 높게** 설정
- [ ] `CB_Status1`(CAN `0x05C` — §8-4 방법 B 필요, 또는 BQStudio)에
      **그 셀 비트만** 서는가
- [ ] 전압을 맞추면 밸런싱이 **꺼지는가**
- [ ] 밸런싱 중 셀 전압 측정값이 **출렁이지 않는가** (밸런싱 전류가 측정에 섞이면 SOC가 튄다)

---

## 9. 단계 8 — 실제 전류 (선택)

여기서만 전자부하/파워서플라이로 진짜 전류를 흘린다.

- [ ] 션트에 알고 있는 전류를 흘려 `Pack_Current` 대조 (§3-3의 전압 주입과 일치하는가)
- [ ] FET ON 상태에서 **게이트 전압 V_GS 측정**: ______ V
      → `R_DS(on)` 6.7 mΩ은 **10 V 기준**. 낮으면 차지펌프 검토
- [ ] 도통 중 FET 케이스 온도

> **전이 조건**: 게이트 전압은 **전력단 설계에 의존**한다. 최종 보드의 FET·차지펌프
> 구성이 다르면 **반드시 재측정**.

---

## 10. 시험 후 정리

- [ ] 실측값을 이 문서에 기입하고 커밋
- [ ] `BMS_LOOP_PERIOD_MS_EST` 를 실측값으로 갱신
- [ ] `PATH_FAULT_CYCLES` 를 G3VM 실측 기준으로 재검토
- [ ] §4에서 틀린 레지스터가 나왔으면 `B_BMS_init.c` 수정
- [ ] **§11 원복표를 전부 되돌렸는가** ← 아래
- [ ] PF 시험 후 `PF_RESET` 수동 전송했는지
- [ ] 8-5-6의 미호출 함수 3개 처리 방침 결정 (호출 추가 / 삭제)
- [ ] 최종 보드에서 재측정할 항목 목록화 (§7-2, §9)

---

## 11. 🚨 시험용 임시 수정 원복표

시험을 위해 코드를 임시로 고치는 곳이 **6군데**다. **하나라도 안 되돌리면 실차에서 사고가 난다.**

| # | 파일 | 상수/코드 | 시험값 | **원래값** | 안 되돌리면 |
|---|---|---|---|---|---|
| 1 | `B_BMS_power_mode.c` | `TIME_1_HOUR_MS` | 30000 | **3600000** | 주행 중 잠깐 무부하에 STOP1 |
| 2 | `B_BMS_power_mode.c` | `TIME_3_DAYS_MS` | 90000 | **259200000** | 🚨 **90초 무부하에 셧다운 — 팩이 죽는다** |
| 3 | `B_BMS_protect.c` | `SOFT_CELL_OV_mV` | 4000 | **4250** | 정상 충전(4.1 V)에 감시자가 BOTHOFF |
| 4 | `B_BMS_protect.c` | `SOFT_TEMP_MAX_C` | 40.0f | **70.0f** | 🚨 **여름철 40 ℃에 팩 차단** |
| 5 | `B_BMS_soc.c` | `BMS_SOC_OCV_REST_MS` | 30000 | **600000** | 정차 30초마다 SOC 튐 |
| 6 | `main.c` | `T_FDCAN_Send_BMS_Data()` 2줄 | 추가 | **없음** | CAN 부하 증가·루프 지연 |

추가로 §5에서 넣었을 수 있는 것:

| # | 파일 | 내용 |
|---|---|---|
| 7 | `main.c` | 루프 주기 측정용 **GPIO 토글** |
| 8 | — | `delayUS` 변경 |

### 원복 확인 방법 ★

눈으로만 확인하면 반드시 빠뜨린다. **기계로 확인한다.**

```bash
# 시험 시작 전 — 원본 보관
git stash list        # 또는 시험용 브랜치를 따로 파고 시험

# 시험 종료 후 — 원본과 다른 곳이 남아 있는지
git diff main --stat
```

- [ ] `git diff` 결과에 **위 8개 중 어느 것도 남아 있지 않은가**
- [ ] 원복 후 **다시 빌드**해서 올렸는가 ← 원복만 하고 안 올리면 의미 없음
- [ ] 원복 빌드로 §8-5-2를 다시 한 번: 무부하 30초에 **STOP1에 안 드는가**
      (1시간이 복구됐다는 증거)

> 💡 **더 안전한 방법**: 시험용 수정을 전부 `#ifdef BENCH_TEST` 로 감싸고
> 빌드 설정에서만 켠다. 그러면 원복이 "매크로 끄기" 하나로 끝나고 빠뜨릴 수 없다.
> 시험을 두 번 이상 할 거면 이 방식을 권한다.

---

## 부록 — 시험 순서 요약

```
1. 통신              ─┐
2. 값 검증            │  여기까지 안 되면 아래가 무의미
3. 레지스터 확인      ─┘

4. 루프 주기 실측    ★  ← 감시자 임계의 기준. 여기서 상수 확정

5. 보호 트립          ─┐
6. 차단 경로 + G3VM  ★ │  코드의 핵심 기능
7. 감시자 (7-8 포함)  ─┘

8. SOC / RTC / 팬 / CAN / 슬립·셧다운 / LED / 밸런싱
9. 실제 전류 (선택)

11. 원복  🚨 ← 빠뜨리면 앞의 전부가 위험해진다
```

**4번을 5~7번보다 먼저** 하는 이유: 감시자의 모든 시간 판정이 루프 주기에서 유도되므로,
주기를 확정하지 않고 감시자를 시험하면 결과를 해석할 수 없다.

**8번 안에서의 순서**: 슬립·셧다운(8-5)을 **맨 마지막**에 한다. 임시 상수 때문에
30초만 무부하여도 잠들어 다른 시험이 방해받는다.
