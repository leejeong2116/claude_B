# 남은 일 목록

지금 당장 못 하거나, 하드웨어·측정·팀 결정이 선행돼야 하는 것들을 한곳에 모은다.
**상세 절차는 각 문서에 있고 여기는 색인이다.**

| 문서 | 무엇이 있나 |
|---|---|
| `EVM_TEST.md` | 보드에 물려서 하는 시험 절차 |
| `HARDWARE_VERIFICATION.md` | 계측기로 재야 하는 항목 |
| `STM32CUBEMX_TODO.md` | CubeMX(`.ioc`)를 열어야 하는 일 · §0 = 다음에 CubeIDE 켜면 할 일 |
| `REGULATION_COMPLIANCE.md` | 2027 BWSC 규정 대조 |

상태 표기 — 🔴 고객/레이스 전 필수 · 🟡 하는 게 좋음 · 🔵 사소

---

## A. 펌웨어만으로 지금 가능 (지시만 있으면 됨)

| # | 할 일 | 근거·비고 |
|---|---|---|
| A1 🟡 | **팬 duty 데드밴드 + 슬루 제한** | 35~55 ℃ 구간 기울기가 4 %/℃ 라 온도 ±0.5 ℃ 노이즈가 duty ±2 %로 그대로 나가고, 63 ms마다 갱신돼 팬이 웅웅거린다. 팩 열 시상수는 분 단위라 63 ms 추종은 의미도 없다. 슬루 제한은 통신두절 시 0→100 % 돌입(0.71 A, 기동 시 2~3배)도 같이 완화한다 |
| A2 🟡 | **OCV 재보정을 덮어쓰기 → 게이트+서서히 섞기** | `B_BMS_soc.c:158` 이 10분 무부하마다 쿨롱 결과를 통째로 덮어쓴다. 21700은 분극이 풀리는 데 30~60분이 걸려 10분 시점 전압은 아직 실제 OCV가 아니다 → 정차 후 SOC가 튄다 |
| A3 🔵 | `B_BMS_soc.c:131` 주석의 `DAConfiguration=0x02` → **`0x06`** 으로 정정 | 실제 설정은 `B_BMS_init.c:157` 의 `0x06`. TRM Table 4-3 확인 결과 `USER_AMPS[1:0]=10 → 10 mA` 라 **×10 환산 결론은 그대로 맞다.** 숫자만 낡았다 |
| A4 🔵 | `EVM_TEST.md` 에 **팬 시험 절 추가** | `STM32CUBEMX_TODO.md §0 ④` 가 "EVM_TEST.md 참조"라고 가리키는데 그 절이 아직 없다 (2026-08-10 작성 시 누락) |
| A5 🟡 | 드라이버 표시용 **요약 경고 비트** 정의 | 규정 2.25.1 은 "energy storage system warnings" 가 주행 중 항상 보이도록 요구한다. 지금 CAN `0x040`/`0x044` 는 SafetyStatus 원시 비트라 디스플레이가 해석해야 한다 |

---

## B. 실측·하드웨어가 선행돼야 함

| # | 할 일 | 왜 지금 못 하나 |
|---|---|---|
| B1 🔴 | **TS1 서미스터 부착 위치 확인** | 온도 임계 전체가 "가장 뜨거운 셀 **표면**"이라는 전제 위에 있다. 공기를 재는 배치면 방전 상한이 80 → 60 ℃ 로 내려가 OTD 65 ℃ 가 초과가 된다. 절차: `EVM_TEST.md` 🌡 절 |
| B2 🔴 | **빌드·플래시** 후 bqStudio로 레지스터 읽기 | PC에서는 문법 검사·오브젝트 컴파일까지만 했다. **링크는 안 해봤다.** 확인할 값: `OCC:Threshold` 12 mV · `OTD:Recovery` 55 ℃ · `OTC:Threshold` 60 ℃. `BQ769x2_SetRegister()` 쓰기는 실패해도 조용하다 |
| B3 🔴 | `BMS_RTC_ClockSource()` == `BMS_RTC_CLK_LSE`(1) 확인 | `IsReady()` 는 LSI 폴백에서도 1이라 판별이 안 된다. LSI면 ±5 % → 3일 임계 ±3.6 h |
| B4 🟡 | **팬 정지→20 % 기동 실측** | 데이터시트에 20 % 동작점(2,400 rpm · 0.07 A)은 있으나 **기동 조건은 없다.** 안 돌면 기동 킥(100 %로 0.5초 후 목표 duty) 추가 |
| B5 🟡 | 팬 두 경우 시험 | 서미스터 분리 → 정지 / 정상 부팅 뒤 I2C 분리 → 100 % |
| B6 🟡 | **덕트 풍향이 실제 발열점에 가는지** | duty 는 셀·FET 온도 최댓값으로 정하는데, 그 지점에 바람이 가는지는 별개다. FET 때문에 100 %로 돌려도 덕트가 셀만 향하면 8.52 W 를 헛쓴다. 최대 부하에서 팬 ON/OFF 온도차로 확인 |
| B7 🔴 | **OCV 테이블 실측 대체** | 현재 13점 중 **끝점 2개만 데이터시트 확정**이고 중간은 일반 NMC 근사다 (`B_BMS_soc.c:45-64`). 50S 데이터시트에 방전 곡선이 없다. 절차: `HARDWARE_VERIFICATION.md §3-1` |
| B8 🟡 | `BQ_CFG_TOP.chg_pump` 확정 | WSC 보고서와 `FETOptions` 주석이 TOP 충전펌프 사용 여부를 다르게 말한다. 하이사이드 게이트 전압 실측 필요 (`B_BMS_init.c` 상단 TODO) |
| B9 🟡 | G3VM 옵토 실측 지연 | SCD 는 450 µs 로 설정돼 있으나 실제 차단 시간은 옵토가 지배한다. `B_BMS_protect.c` 의 `PATH_FAULT_CYCLES`(600 ms) 가 이 값보다 짧으면 정상 트립마다 오탐이 난다 |

---

## C. 하드웨어 변경이 필요 (설계 반영 대상)

| # | 할 일 | 내용 |
|---|---|---|
| C1 🟡 | **팬 태코(rpm) 피드백** | 팬이 죽거나 스톨해도 펌웨어가 모른다. CAN 으로 나가는 건 "명령한 duty"지 실제 회전이 아니다. `BMS_PROJECT.ioc` 전수 확인 결과 **태코 입력 핀이 배정돼 있지 않고** `tim.c` 에 입력 캡처 설정도 없다. 필요: 핀 배정(CubeMX) + 배선 + 입력 캡처 + "duty>0 인데 rpm≈0" 경고. 팬 커넥터에 태코 선이 있는지는 회로도가 이 저장소에 없어 미확인 |
| C2 🔴 | **Safe state contactor** (규정 2.28.1) | 규정은 팩마다 **양극·음극에 contactor 2개**를 요구하고 **MOSFET 은 갈바닉 절연으로 인정하지 않는다.** 우리 CHG/DSG FET 차단과 BOTHOFF 는 셀 보호지 safe state 가 아니다. 2.28.4 에 따라 **MCU 가 contactor 를 닫아 두는 게이트가 되면 안 된다** — e-stop 루프가 직접 끊고 BMS 는 "열 수만" 있어야 한다 |
| C3 🔴 | **절연 감시 장치** (규정 2.27.9) | 차체 절연을 상시 감시하는 **전용 전자장치**가 필요하다. BMS 와 별개이고 우리 코드에 없다 |
| C4 🟡 | 팩 퓨즈 협조 (규정 2.27.10) | 팩마다 퓨즈/차단기, **DC 차단전류**가 최대 단락전류 이상. SCD 200 A · OCD1 65 A 와 정격이 맞물리는지 확인 |
| C5 🔵 | `.ioc` 의 `HSE_VALUE` 8 → 16 MHz 정정 | 현재 MSI 를 쓰므로 무해하지만 HSE 로 바꾸면 클럭이 2배 틀어진다 (`STM32CUBEMX_TODO.md §3-2`) |

---

## D. 팀·규정 판단이 필요 (내가 정하면 안 되는 것)

| # | 항목 | 쟁점 |
|---|---|---|
| D1 🟡 | **OTC 복귀 55 ℃ → 45 ℃?** | 데이터시트 권장 재충전 해제는 <45 ℃ 인데 현재 55 ℃. **필수가 아니라 권장**이고, 내리면 더운 날 충전 재개가 늦어진다 |
| D2 🔴 | **OTD 트립 65 ℃ 재검토** | B1(서미스터 위치) 확정 후. 표면이면 유효, 공기면 60 ℃ 이하로 |
| D3 🔴 | **BQ SHUTDOWN 중 보호 부재** | 규정 2.5.5 는 "at any time" 범위 밖 동작을 금지한다. 무부하 3일 후 BQ 를 끄면 그동안 셀 전압·온도 감시가 없고, 자가방전으로 CUV 아래로 내려가도 못 잡는다. **"차를 꺼둔 상태"를 규정이 말하는 operated 로 볼지**는 해석 문제 → **Scrutineering 에서 질의할 것** |
| D4 🔴 | **HWDF 60초 vs 무기한 수면** | BQ 호스트 워치독은 MCU 가 60초 이상 조용하면 FET 을 연다. `Enter_Sleep_Sequence()` 는 무기한 자려 한다. 해결 경로는 RTC 주기 웨이크업이고 D3 와 같은 뿌리다 |
| D5 🟡 | SOC 분모 노화·온도 보정 | 분모가 신품 Typ. 25,000 mAh 고정이라 셀이 늙으면 SOC 가 낙관적으로 나온다. 저온 가용용량 감소도 미반영. **규정 신고용 용량(제조사 스펙 최댓값)과 SOC 용 실측 용량은 따로 관리할 것** — 2.5.2 계산은 실측값을 쓰지 않는다 |
| D7 🔴 | **OCC 30 A 배포 순서 — 모터 컨트롤러 회생 한계가 선행** | OCC를 50 → 30 A 로 낮추면 회생 제동이 OCC를 훨씬 자주 때린다. 그런데 CHG FET이 열려도 모터는 계속 발전하므로(인버터 바디 다이오드 정류) 버스 전압이 오히려 올라간다. 게다가 `OCCRecoveryThreshold` 가 −1 A 라 **방전으로 돌아설 때에야** 복구돼, 제동 구간 내내 충전 경로가 끊긴다. → **컨트롤러 회생 한계를 30 A 이하로 먼저 설정**한 뒤에 이 펌웨어를 올릴 것. 현재 컨트롤러 설정값은 **미확인** — 구동팀 확인 필요 |
| D6 🔵 | 쿨롱 효율 미반영 | 충전 효율이 100 % 가 아니라 사이클이 쌓이면 SOC 가 위로 편향된다 |

---

## 이미 확인돼 닫힌 것 (다시 파지 말 것)

| 항목 | 결론 |
|---|---|
| 팬 PWM 주파수가 가청 대역 아닌가 | ❌ 문제없음. `tim.c` Prescaler 0 · Period 6399, TIM2 = 160 MHz → **정확히 25.0 kHz** (4선식 팬 규격 21~28 kHz) |
| 20 % duty 로 팬이 도는가 | ✅ 데이터시트에 동작점 명시 — 9WPA0812P4G001 20 %: **2,400 rpm · 0.07 A · 0.84 W · 21 dB** (100 %: 8,250 rpm · 0.71 A · 8.52 W · 54 dB) |
| SOC 가 0~1000 범위를 벗어나는가 | ✅ 클램프 있음 (`B_BMS_soc.c:165-169`) |
| `delta_mAh` 가 MCU 적분인가 | ❌ BQ 의 DASTATUS6 누적 전하를 차분한 값 (`B_BMS_soc.c:133-137`). MCU 루프가 느려도 전하를 놓치지 않는다 |
| OCC/OCD **recovery** 임계 단위 | mA (TRM 13.6.4.3 / 13.6.9.1). `DAConfiguration` 을 따르지 않는 예외 |
| 팩 용량이 규정 11 MJ 를 넘는가 | ❌ 32S5P × 5,000 mAh × 3.6 V = **10.37 MJ** (여유 5.7 %) |
| `main.c` 의 `#include "B_BMS_fan.h"` 가 CubeMX 재생성에 지워지는가 | ❌ `USER CODE BEGIN Includes` 안에 있어 보존됨 |
