# 실측·확인 항목 총괄

지금까지 여러 문서에 흩어져 있던 "직접 재봐야 아는 것"을 한 곳에 모았다.
**장비 기준으로 묶었으므로, 가진 장비에 맞는 절에서 시작하면 된다.**

| 상세 절차가 있는 문서 | 다루는 범위 |
|---|---|
| `EVM_TEST.md` | EVM으로 코드 동작 검증 |
| `HARDWARE_VERIFICATION.md` | 완성 보드로 회로 검증 |
| `STM32CUBEMX_TODO.md` | CubeMX 작업과 재생성 주의 |
| `REGULATION_COMPLIANCE.md` | 2027 BWSC 규정 대조 |

> **원칙**: 지금 코드의 시간 임계값은 **거의 전부 추정 위에 서 있다.**
> 특히 `BMS_LOOP_PERIOD_MS_EST`(300 ms 추정)와 G3VM 전파 지연 두 개가
> 나머지 판정의 전제다. 이 둘을 먼저 재는 것이 순서상 가장 값이 크다.

---

## A. 장비 없이 지금 할 수 있는 것

| # | 항목 | 방법 | 결과를 어디에 |
|---|---|---|---|
| A1 | **모터 컨트롤러 최대 회생 전류** | 컨트롤러 설정/사양서 확인 | 30 A 이하면 `OCC` 30 A 변경 가능 (§D2와 연결) |
| A2 | **모터 컨트롤러 DC 버스 과전압 보호** 임계 | 사양서 | 회생 중 CHG 차단 시 버스가 어디까지 오르면 막히는지 |
| A3 | **인버터 DC 링크 커패시턴스** | 사양서/회로도 | `dV/dt = I/C` 계산에 필요 |
| A4 | **차량 전체 CAN ID 할당표** | 팀 문서 | `0x300`(진단)·`0x040`대와 충돌 없는지 |
| A5 | **팩당 컨택터 2개(+/−) 유무, 하드와이어 여부** | 회로도 | 규정 2.28.1 (MOSFET은 인정 안 됨) |
| A6 | **별도 IMD(절연감시장치) 유무** | 회로도 | 규정 2.27.9 — 없으면 신규 개발 |
| A7 | **팩당 퓨즈의 DC 차단 정격** | 부품 데이터시트 | 규정 2.27.10 (AC용은 인정 안 됨) |
| A8 | **`PWR_EN` 해석 확인** | 회로도 | PA0 → LTC3895 pin34 RUN 이 맞는지 |

---

## B. EVM만 있으면 되는 것

`EVM_TEST.md` 참조. **EVM 2대 권장** (1대면 크로스체크·TOP/BOT 분기가 안 됨).

| # | 항목 | 왜 중요 | 상세 |
|---|---|---|---|
| **B1** | **루프 주기 실측** ★ | `B_BMS_protect.c`의 **모든 시간 임계**가 이 값에서 유도됨 | §6 |
| B2 | BQ 레지스터 덤프 대조 | `SetRegister()`는 실패해도 알 수 없다 | §2 |
| B3 | 스택 전압이 32767에 안 붙는지 | PR #32 수정의 핵심 검증 | §3-2 |
| B4 | 전류 부호·단위 (충전=양수) | 보호 임계 전부가 이 부호에 의존 | §3-3 |
| B5 | TOP `FET_Temp`가 실제로 변하는지 | TS3=FET온도 변경의 검증. OTF의 유일한 소스 | §3-4 |
| B6 | COV/CUV 트립 시 **선택적 차단** | CHG만/DSG만 꺼지는지 (EVM은 반쪽 확인) | §4-1·4-2 |
| B7 | 소프트 백업이 BQ를 앞지르지 않는지 | 앞지르면 오탐 | §4-3 |
| B8 | OTF 트립 | 이전엔 소스가 없어 아예 안 걸리던 항목 | §4-4 |
| **B9** | **OCDL/SCDL 자동 복구** ★ | 안 되면 한 번 걸리고 **영구 락아웃** | §4-5 |
| B10 | PF latch — 조건 해제해도 안 풀리는지 | 풀리면 심각한 버그 | §4-6 |
| B11 | 통신 두절 트립/복구/재시도 3회 latch | | §5-1 |
| B12 | 초기화 중 BOTHOFF 유지 | 한쪽 EVM 뽑고 부팅 | §5-2 |
| B13 | 감시자 개입 중 슬립 금지 | | §5-3 |
| B14 | SOC 부팅값이 0이 아닌지 | 이전엔 항상 0이었음 | §7 |
| B15 | **RTC가 LSE로 도는지** | `BMS_RTC_ClockSource()` == 1. 2면 LSI 폴백(오차 ±5%) | — |
| B16 | 무부하 30초 주기 웨이크업 | HWDF 대책이 작동하는지 | — |
| B17 | 팬 페일세이프 2종 | 서미스터 분리 → **정지** / I2C 분리 → **100%** | — |

> **B1 측정법 (제일 쉬운 것)**: CAN 로거에서 `0x042` 프레임 도착 간격을 본다.
> 그 값을 `BMS_LOOP_PERIOD_MS_EST`에 넣으면 나머지 임계가 전부 따라 움직인다.

---

## C. 완성 보드(PCB)가 있어야 하는 것

`HARDWARE_VERIFICATION.md` 참조.

### C0. 🔴 다른 것보다 먼저 — 서미스터 손상 점검

이전 펌웨어가 **TOP의 DCHG/DDSG를 푸시풀 출력으로 설정**했는데 그 핀엔 서미스터가 붙어 있다.
그 펌웨어를 올려서 전원을 넣은 적이 있으면 **IC가 서미스터를 출력으로 구동한 상태**였다.

- [ ] TOP 보드 DCHG/DDSG 서미스터 저항값 측정 (상온 18 kΩ 계열)
- [ ] 이전 펌웨어 실행 이력 확인

| # | 항목 | 왜 중요 |
|---|---|---|
| **C1** | **G3VM 전파 지연 (t_on/t_off)** ★★ | **아래 별도 절 참조 — 가장 중요** |
| **C2** | **선택적 차단 실동작** ★ | BOT에 `DCHG_HI` → **TOP의 CHG만** 꺼지고 DSG는 살아있어야 함. DSG까지 꺼지면 `TOP.DFETOFF` 설정이 틀린 것 |
| C3 | BOTHOFF 경로 | MCU PA15 → 양쪽 FET OFF |
| C4 | MCU 리셋 중 페일세이프 | 오픈드레인 Hi-Z → 풀업 → BOTHOFF |
| **C5** | **게이트 전압 V_GS** | `R_DS(on)` 6.7 mΩ은 **10 V 기준**. 낮으면 발열 급증 → 차지펌프 판단 |
| C6 | `PWR_EN` → LTC3895 RUN 실측 | 전원 시퀀싱 구현 여부 결정 |
| C7 | 절연 저항 (IMD) | 규정 2.27.9 |
| C8 | BQ 레지스터 재확인 (실보드) | EVM과 배선이 다를 수 있음 |

### 🔴 C1 — G3VM 전파 지연이 왜 제일 중요한가

**이 한 값이 두 가지를 동시에 결정한다.**

```
BOT 내부 판정 → DDSG 핀 → G3VM 광절연 → TOP.DFETOFF → 게이트 방전 → FET OFF
                            ↑ 여기가 지배적
```

| 이 값이 결정하는 것 | 왜 |
|---|---|
| **① SCD 보호가 성립하는가** | SCD는 450 µs로 설정돼 있지만 실제 차단시간은 이 지연이 지배한다. 보고서 5장 SOA는 140 V·100 A에서 **50 µs**를 버틴다고 계산했다. 총 차단시간이 그걸 넘으면 SCD만으로는 단락을 못 막는다 |
| **② 감시자 크로스체크가 오탐을 내는가** | `PATH_FAULT_CYCLES`가 현재 **600 ms**. G3VM 지연이 이보다 길면 **정상 보호 트립마다 "경로 고장"으로 오판**한다 |

**PCB 없이도 잴 수 있다** — G3VM 소자 하나만 브레드보드에 올려서
LED 쪽(1·2번 핀)에 펄스를 넣고 출력(3·4번)을 스코프로 보면 된다.
전체 경로 지연은 여기에 BQ 반응 + 게이트 방전이 더해진 값이므로, **하한은 바로 나온다.**

- [ ] G3VM 단품 t_on: ________ µs
- [ ] G3VM 단품 t_off: ________ µs
- [ ] (보드 있으면) `BOT.DDSG` ↔ `TOP.DFETOFF` 2채널 스코프 실측: ________ µs
- [ ] 총 차단시간 = SCD Delay(450 µs) + BQ 반응 + 위 값 = ________ µs
- [ ] **50 µs SOA 대비 판정** → 넘으면 SCD 임계 하향 또는 2차 보호기/퓨즈에 명시적 위임
- [ ] `PATH_FAULT_CYCLES`(600 ms)가 실측 지연의 **최소 2~3배**인지 확인

---

## D. 실팩 + 부하가 있어야 하는 것

| # | 항목 | 왜 EVM으로 안 되나 |
|---|---|---|
| **D1** | **스택 불일치 오탐** ★ | 셀 시뮬레이터는 전압원이라 부하를 걸어도 전압이 안 떨어진다. 이 오탐은 **급가속으로 팩 전압이 63 ms 사이에 출렁일 때** 생긴다 |
| **D2** | **회생 전류가 30 A를 넘는지** ★ | `OCC` 임계 결정의 전제 (§아래 별도 절) |
| D3 | 실제 팩 전압 ↔ `Stack_Voltage` 대조 | |
| D4 | 100 A 도통 시 FET 발열 | C5(게이트 전압)와 연결 |
| D5 | 서미스터가 팩 최고온 지점에 있는지 | 데이터시트 Note(*2) 요구. 열화상 권장 |
| D6 | 셀 실측 용량 → `BMS_CELL_CAPACITY_mAh` | SOC 정확도 |
| D7 | 실측 OCV 커브 → `ocv_table` | 현재 중간 구간은 근사치 |

### D1 측정법

부하를 급격히 변화시키면서 CAN `0x300` byte 5(`StackMismatchCycles`)를 본다.

- [ ] 정상 주행 중 이 값이 **0을 유지**하는가
- [ ] 급가속·급감속 중에 올라가는가 → 올라가면 `STACK_SUM_TOLERANCE_mV`(1500 mV) 상향 필요

---

## E. 차량 주행이 필요한 것

| # | 항목 | 확인 내용 |
|---|---|---|
| **E1** | **EMI 내성** ★ | 모터 컨트롤러 스위칭 노이즈로 I2C가 흔들리는가 → CAN `0x300` byte 6·7 감시 |
| **E2** | **긴 내리막 지속 회생** ★ | `0x042`의 전류가 **+3000(30 A)** 을 넘는 구간이 있는가 |
| E3 | 소프트 백업 오탐 | 만충~만방 1사이클에서 `0x300` byte 0이 7/8로 뜨지 않는가 |
| E4 | HWDF vs 슬립 | 무부하 방치 시 FET이 열리지 않는가 |
| E5 | 팬 동작 곡선 | 실제 온도에서 duty가 의도대로 오르는가 (`0x043` byte 2) |
| E6 | SOC 정확도 | 만충 후 만방까지 SOC 궤적 |

---

## 🔴 회생 중 CHG 차단 — 왜 순서가 중요한가

`REGULATION_COMPLIANCE.md` §2-1의 배경 설명이다.

### 무슨 일이 일어나나

회생 제동은 모터가 발전기가 되는 것이다. 전류 경로:

```
모터 → 인버터 → DC 링크 커패시터 → BMS CHG FET → 배터리
                      ↑
                 CHG 끊으면 여기서 막힘
```

**모터는 관성으로 계속 돈다.** 인버터는 계속 전류를 밀어내는데 받아줄 데가 없어지면
그 전하가 전부 DC 링크 커패시터에 쌓인다:

```
C · dV/dt = I        →        dV/dt = I / C

예) 회생 30 A, DC 링크 500 µF  →  30 / 500µ = 60,000 V/s = 60 V/ms
```

**밀리초 단위로 수십 V씩 오른다.** 공칭 115 V 버스가 몇 ms 만에 인버터 정격을 넘는다.

### Body diode가 도와주지 않는다

Common-drain 백투백 구성에서 **CHG FET의 body diode는 방전 방향으로만 도통한다.**
충전 방향을 막는 것이 CHG FET의 존재 이유이므로, 꺼지면 진짜로 완전히 막힌다.

동시에 배선 인덕턴스에 흐르던 전류가 끊기며 FET 양단에 `L·di/dt` 스파이크가 걸린다.
이건 TVS나 FET 애벌란시가 받아야 한다 (보고서 5장 SOA).

### 위험도는 상황에 따라 다르다

| 상황 | 회생 전류 | 위험 |
|---|---|---|
| 태양광 어레이 충전만 | ~9 A | 🟢 MPPT가 멈추면 끝 |
| 하드 브레이킹 | 수십 A, 수 초 | 🟡 인버터 DC링크가 감당 |
| **긴 내리막 지속 회생** | 지속 | 🔴 **진짜 문제** |

### ⚠️ 그래서 순서가 반대로 가면 안 된다

> **`OCC`만 30 A로 낮추고 컨트롤러 제한을 안 걸면 오히려 더 위험해진다.**
> 차단이 더 자주 걸리므로 과전압 이벤트가 늘어난다.

**반드시 이 순서로:**

1. **먼저** 모터 컨트롤러 회생 상한을 30 A로 설정 (A1)
2. **그 다음** `OCCThreshold`를 `0x0A` → `0x06` (30 A)

그러면 `OCC`는 정상 운전에서 절대 트립하지 않는 백스톱이 되고, 규정 2.5.6도 만족한다.

> 📌 **규정과의 긴장**: 규정 2.5.6은 "자동으로 막으라"고 하는데, **막는 행위 자체가
> 다른 위험을 만든다.** 해법은 BMS가 끊기 전에 컨트롤러가 먼저 줄이게 하는 것이다.
> BMS 차단은 최후의 수단으로만 동작해야 한다.

---

## 실측 후 결정할 것 — 감시자 2단계 분리

현재 감시자는 **모든 트립에 BOTHOFF(전체 차단)** 로 대응한다. 오탐 위험이 항목마다 다르다:

| 트립 사유 | 오탐 위험 | 근거 |
|---|---|---|
| 경로 고장 (0.6 s) | 🔴 높음 | G3VM 지연이 0.6 s보다 길면 정상 트립마다 오탐 (C1) |
| 스택 불일치 (3 s) | 🔴 높음 | 셀·스택 측정 시점이 다름(63 ms). 부하 변동 시 정상인데 어긋남 (D1) |
| 통신 두절 (1.5 s) | 🟡 중간 | 모터 컨트롤러 노이즈 (E1) |
| PF | 🟢 낮음 | 칩이 확정한 영구 고장 |
| 소프트 백업 | 🟢 낮음 | 임계 여유 + 3~10 s 대기 |

### 검토할 대안 — 등급 분리

| 등급 | 사유 | 동작 |
|---|---|---|
| **즉시 차단** | PF · 소프트 전압/온도 (실제 셀 값이 한계 초과) | BOTHOFF |
| **경고만** | 경로 고장 · 스택 불일치 · 통신 두절 | CAN `0x300` + LED, **차단 안 함** |

**통신 두절을 경고로 내리는 근거**: MCU가 I2C를 놓쳐도 **BQ 칩 두 개는 자기 보호를 계속한다.**
FET도 여전히 BQ 통제 하에 있다. 통신 두절은 "팩이 위험하다"가 아니라 **"MCU가 눈이 멀었다"** 이다.
MCU가 진짜 죽는 경우는 BQ의 HWDF 워치독(60 s)이 잡는다.
시속 90 km/h 공도 주행 중 I2C 글리치로 추진력이 사라지는 것 자체가 위험이다.

**같은 논리로 경로 고장·스택 불일치도** "지금 팩이 위험하다"가 아니라 **"진단이 안 맞는다"** 이다.
감시자의 목적은 **차단 경로가 고장났음을 알리는 것**이지 즉시 끊는 것이 아니다.
진짜 위험한 상황이면 BQ가 알아서 끊는다.

- [ ] C1·D1·E1 실측 후 등급 분리 여부 결정
- [ ] 분리한다면 `B_BMS_protect.c`에 경고 등급 추가

---

## 우선순위 요약

| 순서 | 항목 | 필요 장비 | 왜 먼저 |
|---|---|---|---|
| 1 | A1~A8 (문서 확인) | 없음 | 지금 당장 가능. 여러 결정의 전제 |
| 2 | **C1 G3VM 지연** | G3VM 단품 + 스코프 | 두 가지 판정의 전제. PCB 없어도 가능 |
| 3 | **B1 루프 주기** | EVM + CAN 로거 | 모든 시간 임계의 기준 |
| 4 | B2~B17 | EVM | 코드 검증 대부분 |
| 5 | C0 서미스터 손상 | 보드 + 멀티미터 | 손상됐으면 위 온도 시험이 무의미 |
| 6 | C2~C8 | 완성 보드 | 절연 경로 실동작 |
| 7 | D1~D7 | 실팩 + 부하 | |
| 8 | E1~E6 | 차량 | |
